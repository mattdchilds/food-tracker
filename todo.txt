[DONE 2026-07-23] Permission rework: no more surprise prompt each session; passive banner + pick "Allow on every visit" in Chrome's prompt once = silent forever after
[DONE 2026-07-23] Relaxation score chip added at top (green 🔓 0, orange when >0; resets with each day / Reset Day)
-Add new feature - track over last 7 days. I have two goals I want based around this - "Cost" (each meal has a $ amount), and "Variety", which ensures some food Variety. Days not tracked to completion (90%+ towards "all goals met") shouldn't be "seen", and it goes back farther. There should be a "reset weekly" button to reset.
-Cost feature - There should be an "amount spent" over past 7 days tracked, using tracked history.
-Variety - A new feature I'd like. Some way to ensure healthy variety. But foods would need to have some kind of way to show similarity to others - For example - Pizza slice large and pizza slice medium aren't variety. Oatmeal and black beans are. Huel and protein fluff might have some overlap. Instead of just coding this in, make a plan first and discuss it with user.
[DONE 2026-07-23] Goals now have an On/Off toggle in the ⚙ Goals modal (Calories can't be turned off — it anchors the solver)